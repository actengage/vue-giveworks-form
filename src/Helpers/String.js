export default {
    /*
    ltrim: function(str, char) {
        return str.replace(new RegExp(char + '+$'), '');
    },

    rtrim: function(str, char) {
        return str.replace(new RegExp(char + '+$'), '');
    }
    */        
}
